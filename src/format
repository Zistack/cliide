for file in $(find . -type f -regex '.*\.[ch]pp$')
do
	
	if ! clang-format ${file} | diff -q ${file} - > /dev/null
	then
		
		tmpfile=$(mktemp)
		clang-format ${file} > ${tmpfile}
		cat ${tmpfile} > ${file}
		rm ${tmpfile}
		
	fi
	
done
